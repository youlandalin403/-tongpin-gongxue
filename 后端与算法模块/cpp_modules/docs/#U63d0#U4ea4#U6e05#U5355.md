# C++ 模块提交清单

提交给组长和 Python 同学时，建议提交整个 `cpp_modules_submit` 文件夹或压缩包。

必须包含：

```text
algorithm.cpp
algorithm.exe（在 Windows 本机编译后生成）
input_samples/
result_samples/
docs/C++模块调用说明.md
docs/C++模块自测报告.md
```

辅助文件：

```text
build_windows.bat
build_gcc.bat
build_and_test_windows.bat
test_all.bat
python_demo/cpp_runner_example.py
```

群里说明：

```text
我这边 C++ 模块已完成自测，支持 match / score / ranking / validate 四种模式。统一调用格式为：
algorithm.exe mode input.json result.json

C++ 不连接 SQLite，不处理页面，只读取 JSON 输入并输出 JSON 结果。已附输入样例、输出样例、调用说明和自测报告。
```
